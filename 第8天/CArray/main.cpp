#include<iostream>
#include"Worker.h"
using namespace std;
int main()
{
	/*CArray<int> array1;
	CArray<int>array2;
	array2.Add(4);
	array2.Add(5);
	array2.Add(6);
	array1.Add(1);
	array1.Add(2);
	array1.Add(3);
	array1.Add(4);
	array1.Add(5);
	for (int i = 0; i < array1.GetCount(); i++)
		cout << array1.GetAt(i) << ' ';
	cout << endl;
	CArray<int>array3;
	array3 = array1;
	for (int i = 0; i < array3.GetCount(); i++)
		cout << array3.GetAt(i) << ' ';
	cout << endl;*/
	CWorker worker;
	worker.Into();
	return 0;
}